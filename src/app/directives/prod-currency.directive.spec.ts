import { ProdCurrencyDirective } from './prod-currency.directive';

describe('ProdCurrencyDirective', () => {
  it('should create an instance', () => {
    const directive = new ProdCurrencyDirective();
    expect(directive).toBeTruthy();
  });
});
